# Environment Variables to be used during deployment
export USERNAME="" # username for remote workers
export CIDR=""     # Example Network CIDR="10.244.0.0/16" for pod network

# Fetch the most current Kubernetes version from the official release page
#  apt-cache madison kubeadm kubelet kubectl 
KUBEVERSION="1.28.2-00"
